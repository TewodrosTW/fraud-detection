Structure:

- raw/: raw incoming data (gitignored)
- processed/: cleaned/processed datasets for modeling

Do not commit large data files to the repository; use an external data store or dataset registry where possible.
